import { Button } from '@/components/form/Button/Button'
import styled from 'styled-components'

export const SecondaryButton = styled(Button)
  .attrs({ textColor: 'link', align: 'center', width: 'full', color: 'white', size: 'medium' })``

